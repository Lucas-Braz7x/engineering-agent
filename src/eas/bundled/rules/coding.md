# Coding Rules

1. Match existing style, naming, and layout in the repository.
2. Keep changes scoped to the feature under review.
3. Public CLI behavior must stay backward compatible unless the requirement says otherwise.
4. Use type hints on new Python code (3.11+ syntax where applicable).
5. Add or update tests when behavior changes; follow patterns in `tests/`.
