# Integração com Cursor / Claude

[← Índice](../doc.md)

Inicialmente o EAS **não** precisa substituir Cursor ou Claude Code — pode coexistir.

```text
Engineering Agent System
          ↓
     .ai/ context
          ↓
Cursor / Claude
          ↓
      Repository
```

Os arquivos `.md` funcionam como contexto para agentes existentes — valida a arquitetura antes do runtime completo.
